amends "package://github.com/jdx/hk/releases/download/v1.50.0/hk@1.50.0#/Config.pkl"

hooks {
  fix = true
  ["pre-commit"] {
    steps {
      ["oxfmt"] {
        check_first = true
        check = "./node_modules/.bin/oxfmt --check --no-error-on-unmatched-pattern {{files}}"
        fix = "./node_modules/.bin/oxfmt --write {{files}}"
      }
      ["oxlint"] {
        check_first = true
        check = "./node_modules/.bin/oxlint --no-error-on-unmatched-pattern {{files}}"
        fix = "./node_modules/.bin/oxlint --fix {{files}}"
      }
    }
  }
}
